# .env.example
# Copy this file to .env and fill in your values.
# .env is git-ignored — never commit real secrets.
#
# Usage: the scripts in .agent/scripts/ load this file automatically.
# Alternatively, set variables in your shell environment before running scripts.

# ─── Jules AI (Google) ────────────────────────────────────────────────────────
# Required by: .agent/scripts/jules-api.sh, .agent/scripts/jules-batch-review.js
# Get your key at: https://jules.google.com → Settings → API Keys
JULES_API_KEY=

# ─── GitHub CLI ───────────────────────────────────────────────────────────────
# No token needed here. The `gh` CLI authenticates separately via:
#   gh auth login
# Run this once in your terminal. All scripts using `gh` will pick it up.
# Optional: set GH_TOKEN if you need non-interactive/CI usage.
# GH_TOKEN=

# ─── GitHub Projects (gh-helpers.sh) ─────────────────────────────────────────
# Project board settings are configured directly in .agent/scripts/gh-helpers.sh
# (OWNER, PROJECT_NUM, PROJECT_ID, FIELD_ID) — not secrets, just config values.

# ─── Add your project-specific secrets below ──────────────────────────────────
# [FILL: any additional API keys your project needs]
# EXAMPLE_API_KEY=
